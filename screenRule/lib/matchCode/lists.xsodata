//oData responsible for listing the match codes from VTEX

service {

//Calculation view (recep) "sku":
"cvqtSku" as "sku"  with("id", "productId", "isActive") key("id") 
parameters via entity;

//Calculation view (recep) "saleType":
"cvqtSaleType" as "saleType" with("id", "saleType", "status") key("id") 
parameters via entity;

//Calculation view (recep) "productRule":
"cvqtProductRule" as "productRule";

}