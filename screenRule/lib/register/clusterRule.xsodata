service {

//cvqClusterRule
"cvqClusterRule" as "ClusterRule";

//tbRestrictionRule
"restrictionRule.restrictionRule" as "restrictionRule" key("idRule","idRestriction") 
   create 
   update;

//tbCluster
"cluster.cluster" as "cluster" key("id")
    create 
    update;
    
  //tbClusterRule
"clusterRule.clusterRule" as "clusterRule" key("id")
    create 
    update;
}