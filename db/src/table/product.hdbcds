using types;
context product {

	entity product{
		key	id                : types.id;
			productId         : types.productId;
			productCategories : types.productCategories;
			IsActive : types.isActive;
	};
};